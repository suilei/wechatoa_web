<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
       http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.1.xsd
       http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.1.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd
       ">
       
    <!--  启动注解扫描 -->   
    <context:component-scan base-package="com">
<!--    	 <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>-->
   	</context:component-scan> 
       
	<!-- 加载资源文件 -->
	<context:property-placeholder location="classpath:db.properties"/>
	
	<bean id="writeDataSource" class="org.logicalcobwebs.proxool.ProxoolDataSource">
		<property name="alias" value="writeDataSource"/>
		<property name="driver" value="${write.connection.driver_class}" />
		<property name="driverUrl" value="${write.connection.url}" />
		<property name="user" value="${write.connection.username}" />
		<property name="password" value="${write.connection.password}" />
		<property name="maximumConnectionCount" value="${write.proxool.maximum.connection.count}"/>
		<property name="minimumConnectionCount" value="${write.proxool.minimum.connection.count}" />
		<property name="statistics" value="${write.proxool.statistics}" />
		<property name="simultaneousBuildThrottle" value="${write.proxool.simultaneous.build.throttle}"/>
	</bean>
    
    <bean id="readDataSource1" class="org.logicalcobwebs.proxool.ProxoolDataSource">
        <property name="alias" value="readDataSource1"/>
        <property name="driver" value="${read1.connection.driver_class}" />
        <property name="driverUrl" value="${read1.connection.url}" />
        <property name="user" value="${read1.connection.username}" />
        <property name="password" value="${read1.connection.password}" />
        <property name="maximumConnectionCount" value="${read1.proxool.maximum.connection.count}"/>
        <property name="minimumConnectionCount" value="${read1.proxool.minimum.connection.count}" />
        <property name="statistics" value="${read1.proxool.statistics}" />
        <property name="simultaneousBuildThrottle" value="${read1.proxool.simultaneous.build.throttle}"/>
    </bean>
    
     <bean id="readDataSource2" class="org.logicalcobwebs.proxool.ProxoolDataSource">
        <property name="alias" value="readDataSource2"/>
        <property name="driver" value="${read2.connection.driver_class}" />
        <property name="driverUrl" value="${read2.connection.url}" />
        <property name="user" value="${read2.connection.username}" />
        <property name="password" value="${read2.connection.password}" />
        <property name="maximumConnectionCount" value="${read2.proxool.maximum.connection.count}"/>
        <property name="minimumConnectionCount" value="${read2.proxool.minimum.connection.count}" />
        <property name="statistics" value="${read2.proxool.statistics}" />
        <property name="simultaneousBuildThrottle" value="${read2.proxool.simultaneous.build.throttle}"/>
    </bean>
    
     <bean id="readWriteDataSource" class="com.panguso.ssd.rec.weibo.datasource.ReadWriteDataSource">
        <property name="writeDataSource" ref="writeDataSource"/>
        <property name="readDataSourceMap">
           <map>
              <entry key="readDataSource1" value-ref="readDataSource1"/>
              <entry key="readDataSource2" value-ref="readDataSource2"/>
<!--              <entry key="readDataSource3" value-ref="readDataSource2"/>-->
<!--              <entry key="readDataSource4" value-ref="readDataSource2"/>-->
           </map>
        </property>
    </bean>

    <bean id="readWriteDataSourceTransactionProcessor" class="com.panguso.ssd.rec.weibo.datasource.ReadWriteDataSourceProcessor">
	<!-- 设置为true，则当有写操作时，强制从读库读取 -->
<!--       <property name="forceChoiceReadWhenWrite" value="true"/>-->
	<!-- 设置为false，则当有写操作时，从写库进行读取 -->
       <property name="forceChoiceReadWhenWrite" value="false"/>
    </bean>
    
    <bean id="txManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="readWriteDataSource"/>
    </bean>
    
    <!-- 事务属性配置 -->
	<tx:advice id="txAdvice" transaction-manager="txManager">
		<tx:attributes>
		    <!-- 方法对应的传播属性 -->
			 <tx:method name="save*" propagation="REQUIRED" />
            <tx:method name="add*" propagation="REQUIRED" />
            <tx:method name="create*" propagation="REQUIRED" />
            <tx:method name="insert*" propagation="REQUIRED" />
            <tx:method name="update*" propagation="REQUIRED" />
            <tx:method name="merge*" propagation="REQUIRED" />
            <tx:method name="del*" propagation="REQUIRED" />
            <tx:method name="remove*" propagation="REQUIRED" />
            
            <tx:method name="put*" read-only="true"/>
            <tx:method name="query*" read-only="true"/>
            <tx:method name="use*" read-only="true"/>
            <tx:method name="get*" read-only="true" />
            <tx:method name="count*" read-only="true" />
            <tx:method name="find*" read-only="true" />
            <tx:method name="list*" read-only="true" />
            
            <tx:method name="*" propagation="REQUIRED"/>
		</tx:attributes>
	</tx:advice>
	
	<!-- 事务控制位置，一般在业务层service -->
	<aop:config expose-proxy="true">
        <!-- 只对业务逻辑层实施事务 -->
        <aop:pointcut id="txPointcut" expression="execution(* com.panguso.ssd.rec.weibo..service..*.*(..))" />
        <aop:advisor advice-ref="txAdvice" pointcut-ref="txPointcut"/>
        <aop:aspect order="-2147483648" ref="readWriteDataSourceTransactionProcessor">
           <aop:around pointcut-ref="txPointcut" method="determineReadOrWriteDB"/>
        </aop:aspect>
    </aop:config>
</beans>